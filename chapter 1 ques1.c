#include<stdio.h>
main()
{
    float b,g;
    printf("Enter the basic salary ");
    scanf("%f",&b);
    g=b+b*0.4+b*0.2;
    printf("\nGross salary of the person is %f",g);
    /* code to find gross salary when basic salary is entered though keyboard.*/
}
